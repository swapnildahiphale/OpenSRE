{{- /*
HorizontalPodAutoscalers (optional per service).
*/ -}}

{{- if and .Values.services.configService.enabled .Values.services.configService.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: opensre-config-service
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: opensre-config-service
  minReplicas: {{ .Values.services.configService.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.configService.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.configService.hpa.targetCPUUtilizationPercentage }}
{{- end }}

{{- if and .Values.services.orchestrator.enabled .Values.services.orchestrator.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: opensre-orchestrator
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: opensre-orchestrator
  minReplicas: {{ .Values.services.orchestrator.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.orchestrator.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.orchestrator.hpa.targetCPUUtilizationPercentage }}
{{- end }}

{{- if and .Values.services.agent.enabled .Values.services.agent.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: opensre-agent
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: opensre-agent
  minReplicas: {{ .Values.services.agent.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.agent.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.agent.hpa.targetCPUUtilizationPercentage }}
  {{- if .Values.services.agent.hpa.behavior }}
  behavior:
    {{- if .Values.services.agent.hpa.behavior.scaleUp }}
    scaleUp:
      stabilizationWindowSeconds: {{ .Values.services.agent.hpa.behavior.scaleUp.stabilizationWindowSeconds | default 0 }}
      policies:
        {{- range .Values.services.agent.hpa.behavior.scaleUp.policies }}
        - type: {{ .type }}
          value: {{ .value }}
          periodSeconds: {{ .periodSeconds }}
        {{- end }}
    {{- end }}
    {{- if .Values.services.agent.hpa.behavior.scaleDown }}
    scaleDown:
      stabilizationWindowSeconds: {{ .Values.services.agent.hpa.behavior.scaleDown.stabilizationWindowSeconds | default 300 }}
      policies:
        {{- range .Values.services.agent.hpa.behavior.scaleDown.policies }}
        - type: {{ .type }}
          value: {{ .value }}
          periodSeconds: {{ .periodSeconds }}
        {{- end }}
    {{- end }}
  {{- end }}
{{- end }}

{{- if and .Values.services.aiPipelineApi.enabled .Values.services.aiPipelineApi.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: opensre-ai-pipeline-api
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: opensre-ai-pipeline-api
  minReplicas: {{ .Values.services.aiPipelineApi.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.aiPipelineApi.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.aiPipelineApi.hpa.targetCPUUtilizationPercentage }}
{{- end }}

{{- if and .Values.services.webUi.enabled .Values.services.webUi.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: opensre-web-ui
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: opensre-web-ui
  minReplicas: {{ .Values.services.webUi.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.webUi.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.webUi.hpa.targetCPUUtilizationPercentage }}
{{- end }}

{{- if and .Values.services.k8sGateway.enabled .Values.services.k8sGateway.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: opensre-k8s-gateway
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: opensre-k8s-gateway
  minReplicas: {{ .Values.services.k8sGateway.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.k8sGateway.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.k8sGateway.hpa.targetCPUUtilizationPercentage }}
{{- end }}


