.env
litellm_config.yaml

.venv
__pycache__/
.pytest_cache/
*.egg-info/
.ruff_cache/

# Conductor workspace files
.context/

# Terraform
**/.terraform/
**/terraform.tfstate
**/terraform.tfstate.backup
**/*.tfstate
**/*.tfstate.backup
**/terraform.tfvars
**/terraform.tfvars.json
**/*.tfvars
**/tfplan
**/*.tfplan

# Docker temp auth used by deploy script
config_service/.docker-tmp/

# Local scratch / exported secrets (do not commit)
database/local/tmp_secrets
database/local/rds_commands

# Knowledge base generated artifacts / caches (can be regenerated)
knowledge_base/**/.cache/
knowledge_base/**/.cache/**
knowledge_base/**/runs/
knowledge_base/**/runs/**
knowledge_base/**/*.pkl
knowledge_base/**/*.sqlite

# Ultimate RAG caches (RAPTOR embedding/summary caches)
.cache/
ultimate_rag/**/.cache/
**/*.sqlite-shm
**/*.sqlite-wal


eval_results_*.json

# Cached test data (regenerated on demand)
local/data/

# Mac OS artifacts
.DS_Store
**/.DS_Store

# Local agent/tool scratch output (screenshots, playwright dumps, etc.)
output/

# Accidental nested clone of the public repo into this private tree
/OpenSRE/

# Claude Code / IDE config (root-level only, not sre-agent/.claude/skills/)
/.claude/
website/launch/.claude/
/.cursor/
/.codex/
AGENTS.md

# Node.js
**/node_modules/

# Next.js
**/.next/
**/out/

note.md

# Large generated files
knowledge_base/trees/mega_ultra_v2/tree_explorer.html
knowledge_base/trees/mega_ultra_v2/keywords.json

# Generated kubeconfigs with embedded certs/tokens (regenerated by setup scripts)
test-infra/kubeconfig-docker.yaml
test-infra/kind/kubeconfig-docker.yaml
test-infra/eks/kubeconfig-eks.yaml
.credentials-opensre-launch.md
.vercel

# Tool artifacts
.entire/
.playwright-cli/
.playwright-mcp/
.gemini/
.superpowers/

# Large video/media files
video-analysis-frames/
OpenSRE*.mp4

# Local isolated validation stack (agent-prompt-edit) — never commit
docker-compose.promptfix.yml

# Local isolated validation stack (claude-agent-sdk upgrade) — never commit
docker-compose.sdkupgrade.yml

# Local isolated validation stack (agent-run UI) — temporary, removed
docker-compose.agentui.yml

# Self-hosted K8s deploy overlays (private repo; deploy/ excluded from public sync)
# Local scratch copy only — use deploy/values.site.<cluster>.yaml for committed overlays
deploy/values.site.yaml
deploy/values.*.yaml
!deploy/values.site.*.yaml

# Temporary Cursor canvas mockups (worktree copies; canonical under .cursor/projects/.../canvases/)
canvases/

# Retry/build script logs (e.g. scripts/retry-sre-agent-build-push.sh)
.retry-*.log

# Helm subchart packages (regenerated by `helm dependency update charts/opensre`)
charts/opensre/charts/*.tgz
