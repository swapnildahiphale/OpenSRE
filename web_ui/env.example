# Example environment (copy to `.env.local` for Next.js)
#
# Required: where the Web UI proxies API calls upstream (identity/config/admin).
# - Local dev (config service running locally): http://localhost:8080
# - Via SSM tunnel to internal ALB:            http://localhost:8081
CONFIG_SERVICE_URL=http://localhost:8080

# Agent Service (multi-agent AI execution - Planner + sub-agents)
# Used for /api/team/agent/stream endpoint (AgentRunnerModal)
# Local dev: docker-compose maps agent service to 8081
AGENT_SERVICE_URL=http://localhost:8081

# Orchestrator (team provisioning control plane; internal-only)
# Only used for admin operations, not for agent execution
ORCHESTRATOR_URL=http://localhost:8070

# RAPTOR Knowledge Base API (tree explorer, semantic search, Q&A)
# - Local dev: http://localhost:8000
# - Production: internal ALB URL
RAPTOR_API_URL=http://localhost:8000

# Cookie settings
# In prod (HTTPS), set this to 1 so the session cookie is `Secure`.
# For local dev over http://localhost (SSM tunnel), keep it 0.
WEB_UI_COOKIE_SECURE=0

# OIDC login (recommended for enterprise)
# When enabled, the UI exposes:
# - GET /api/auth/login    (redirect to IdP, PKCE)
# - GET /api/auth/callback (code exchange, validate via config_service, set session cookie)
NEXT_PUBLIC_WEB_UI_OIDC_ENABLED=0
WEB_UI_OIDC_ENABLED=0
WEB_UI_PUBLIC_BASE_URL=http://localhost:3000
WEB_UI_OIDC_AUTHORIZATION_ENDPOINT=
WEB_UI_OIDC_TOKEN_ENDPOINT=
WEB_UI_OIDC_CLIENT_ID=
WEB_UI_OIDC_CLIENT_SECRET=
WEB_UI_OIDC_SCOPES="openid email profile groups"


