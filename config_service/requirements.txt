# Core dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0  # Required for pydantic EmailStr
pyyaml>=6.0
ruamel.yaml>=0.18.0  # For YAML config with comment preservation
watchdog>=3.0.0  # For file watching in local mode
structlog>=23.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# API
fastapi>=0.115.0
uvicorn>=0.30.0

# CLI
click>=8.0.0
rich>=13.0.0

# HTTP / Metrics
httpx>=0.27.0
prometheus-client>=0.19.0

# Auth (OIDC/JWT)
PyJWT>=2.8.0
cryptography>=41.0.0

# Cron expression parsing for scheduled jobs
croniter>=2.0.0

# Optional cache (enabled via CONFIG_CACHE_BACKEND=redis)
redis>=5.0.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
ruff>=0.1.0
mypy>=1.0.0
